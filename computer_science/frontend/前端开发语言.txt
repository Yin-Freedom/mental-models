
React
Vue

看elementui的upload源码时，发现其使用vue的render函数实现页面渲染，而不是template模板语法。
这种写法的好处类似于React使用JSX，（1）逻辑是视图融合（2）编译能检查视图中的类型错误。
其次，React使用虚拟DOM，而不是视图模板语法直接操作DOM，好处
（1）减少DOM操作，仅在需要时更新差异部分，提升性能
（2）灵活程度高，可以实现动态子组件、自定义slot
React成为主流前端框架的核心原因：
（1）虚拟DOM，高效
（2）组件化
（3）声明式编程，避免状态驱动视图更新
（4）单向数据流【？】

